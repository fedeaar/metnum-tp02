proc deflacion(in A: matriz<n, n>, in q: Nat, in k: Nat, in t: Real) {

    eigvals := vector<q>
    eigvecs := matriz<n, q>
    
    i := 0
    while i < q {

        l, V := potencia(A, k, t)
        eigvals[i] := l
        eigvecs.columna[i] := V

        A := A - l * (V * V.t)
        i := i + 1
    }
    
    return eigvals, eigvecs
}
